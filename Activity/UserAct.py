class UserAct:
    def __init__(self, name, user_id, activity_id, cal, number_min, data):
        self.name, self.user_id, self.activity_id, self.cal, self.number_min, self.data = \
        name, user_id, activity_id, cal, number_min, data