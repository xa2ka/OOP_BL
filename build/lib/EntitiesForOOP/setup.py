from setuptools import setup, find_packages

setup(
    name='mylibrary',
    version='1.0.0',
    packages=find_packages(),
    author='Xatura',
    description='For my OOP Project',
    # Другие настройки, если необходимо
)